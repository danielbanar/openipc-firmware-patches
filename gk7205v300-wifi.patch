diff --git a/br-ext-chip-goke/configs/gk7205v300_ultimate_defconfig b/br-ext-chip-goke/configs/gk7205v300_ultimate_defconfig
index 8dd5a190..0d579946 100644
--- a/br-ext-chip-goke/configs/gk7205v300_ultimate_defconfig
+++ b/br-ext-chip-goke/configs/gk7205v300_ultimate_defconfig
@@ -81,3 +81,6 @@ BR2_PACKAGE_UACME_OPENIPC=y
 BR2_PACKAGE_VTUND_OPENIPC=y
 BR2_PACKAGE_YAML_CLI=y
 BR2_PACKAGE_ZEROTIER_ONE=y
+BR2_PACKAGE_RTL8733BU_OPENIPC=y
+BR2_PACKAGE_IW=y
+BR2_PACKAGE_NANO=y
diff --git a/general/overlay/etc/init.d/S40network b/general/overlay/etc/init.d/S40network
index b8fd1093..69fa7a4a 100755
--- a/general/overlay/etc/init.d/S40network
+++ b/general/overlay/etc/init.d/S40network
@@ -13,17 +13,16 @@ set_wireless() {
 		ifup usb0
 		ifup eth1
 	fi
-	[ -e /sys/class/net/eth0 ] && ifconfig eth0 "${net:-192.168.2.10}" hw ether "${eth:-00:00:23:34:45:66}" up
 }
 
 start() {
-	echo "Starting network..."
+	echo "Starting wireless..."
 	ifup lo
 	if [ -n "$dev" ]; then
 		set_wireless
-	else
-		ifup eth0
 	fi
+	echo "Starting ethernet..."
+	ifup eth0
 }
 
 stop() {
diff --git a/general/overlay/etc/network/interfaces.d/wlan0 b/general/overlay/etc/network/interfaces.d/wlan0
index 7bce98b9..69b8f66a 100644
--- a/general/overlay/etc/network/interfaces.d/wlan0
+++ b/general/overlay/etc/network/interfaces.d/wlan0
@@ -1,5 +1,3 @@
 iface wlan0 inet dhcp
-    pre-up wpa_passphrase "$(fw_printenv -n wlanssid)" "$(fw_printenv -n wlanpass)" > /tmp/wpa_supplicant.conf
-    pre-up sed -i 's/#psk.*/scan_ssid=1/g' /tmp/wpa_supplicant.conf
-    pre-up wpa_supplicant -B -i wlan0 -D nl80211,wext -c /tmp/wpa_supplicant.conf
-    post-down killall -q wpa_supplicant
+	pre-up wpa_supplicant -B -i wlan0 -D nl80211,wext -c /etc/wpa_supplicant.conf
+	post-up iw dev wlan0 set txpower fixed 1000
diff --git a/general/overlay/etc/wifi b/general/overlay/etc/wifi
new file mode 100644
index 00000000..6caa3a2a
--- /dev/null
+++ b/general/overlay/etc/wifi
@@ -0,0 +1 @@
+enabled=true
\ No newline at end of file
diff --git a/general/overlay/etc/wpa_supplicant.conf b/general/overlay/etc/wpa_supplicant.conf
new file mode 100644
index 00000000..3a382a4e
--- /dev/null
+++ b/general/overlay/etc/wpa_supplicant.conf
@@ -0,0 +1,10 @@
+network={
+	ssid="SSID1"
+	psk="012345789"
+	priority=20
+}
+network={
+	ssid="SSID2"
+	psk="0123456789"
+	priority=10
+}
diff --git a/general/overlay/usr/share/udhcpc/default.script b/general/overlay/usr/share/udhcpc/default.script
index 925137f9..548c6119 100755
--- a/general/overlay/usr/share/udhcpc/default.script
+++ b/general/overlay/usr/share/udhcpc/default.script
@@ -50,13 +50,6 @@ case "$1" in
 		fi
 		;;
 
-	leasefail|nak)
-		if [ -x /usr/sbin/avahi-autoipd ]; then
-			/usr/sbin/avahi-autoipd -c $interface || /usr/sbin/avahi-autoipd -wD $interface --no-chroot
-		fi
-		/sbin/ifconfig $interface $(fw_printenv -n ipaddr || echo -n 192.168.1.10) netmask $(fw_printenv -n netmask || echo -n 255.255.255.0) up
-		;;
-
 	renew|bound)
 		if [ -x /usr/sbin/avahi-autoipd ]; then
 			/usr/sbin/avahi-autoipd -c $interface && /usr/sbin/avahi-autoipd -k $interface
